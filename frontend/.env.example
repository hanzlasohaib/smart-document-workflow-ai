# Public API base (browser → FastAPI /api/v1)
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# Server-side BFF → FastAPI (Compose may use http://api:8000/api/v1)
API_INTERNAL_URL=http://localhost:8000/api/v1

# Set true behind HTTPS so refresh cookie gets Secure flag
COOKIE_SECURE=false
